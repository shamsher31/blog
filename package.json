{
  "name": "Blog",
  "version": "1.0.0",
  "description": "Welcome to my personal blog",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "docs:prepare": "gitbook install",
    "docs:watch": "npm run docs:prepare && gitbook serve",
    "docs:build": "npm run docs:prepare && rm -rf _book && gitbook build",
    "docs:publish": "npm run docs:build && cd _book && git init && git commit --allow-empty -m 'Update docs' && git checkout gh-pages && git add . && git commit -am 'Update docs' && git push git@github.com:shamsher31/blog gh-pages --force"
  },
  "author": "Shamsher Ansari (https://github.com/shamsher31)",
  "license": "ISC",
  "devDependencies": {
    "gitbook-cli": "^1.0.0"
  }
}